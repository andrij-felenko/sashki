import 'package:flutter/material.dart';

class FishkaItem extends StatefulWidget {
  const FishkaItem({
    super.key,
  });

  @override
  State<FishkaItem> createState() => _FishkaItem();
}

class _FishkaItem extends State<FishkaItem> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}